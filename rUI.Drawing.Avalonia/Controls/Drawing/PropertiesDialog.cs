using rUI.Avalonia.Desktop.Controls;

namespace rUI.Drawing.Avalonia.Controls.Drawing;

public sealed class PropertiesDialog : ContentDialog
{
    protected override Type StyleKeyOverride => typeof(ContentDialog);
}
