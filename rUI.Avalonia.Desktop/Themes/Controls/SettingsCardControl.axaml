<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="using:rUI.Avalonia.Desktop.Controls">

    <ControlTheme x:Key="{x:Type controls:SettingsCardControl}" TargetType="controls:SettingsCardControl">
        <Setter Property="Template">
            <ControlTemplate>
                <Border x:Name="PART_Root"
                        Background="{DynamicResource rUISurfaceBrush}"
                        BorderBrush="{DynamicResource rUIBorderSubtleBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <Grid ColumnDefinitions="Auto,*,Auto">

                        <!-- Icon container -->
                        <Border Grid.Column="0"
                                Width="40" Height="40"
                                VerticalAlignment="Center"
                                IsVisible="{Binding IconData, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static ObjectConverters.IsNotNull}}">
                            <PathIcon Data="{Binding IconData, RelativeSource={RelativeSource TemplatedParent}}"
                                      Width="20" Height="20"
                                      Foreground="{DynamicResource rUIForegroundBrush}" />
                        </Border>

                        <!-- Header + Description -->
                        <StackPanel Grid.Column="1"
                                    VerticalAlignment="Center"
                                    Margin="12,0,12,0">
                            <TextBlock Text="{Binding Header, RelativeSource={RelativeSource TemplatedParent}}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource rUIForegroundBrush}" />
                            <TextBlock Text="{Binding Description, RelativeSource={RelativeSource TemplatedParent}}"
                                       FontSize="12"
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource rUIForegroundSecondaryBrush}"
                                       IsVisible="{Binding Description, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                        </StackPanel>

                        <!-- Chevron -->
                        <PathIcon Grid.Column="2"
                                  Width="12" Height="12"
                                  VerticalAlignment="Center"
                                  Data="M8.59 16.59L13.17 12 8.59 7.41 10 6l6 6-6 6z"
                                  Foreground="{DynamicResource rUIForegroundBrush}" />

                    </Grid>
                </Border>
            </ControlTemplate>
        </Setter>

        <Style Selector="^:pointerover /template/ Border#PART_Root">
            <Setter Property="Background" Value="{DynamicResource rUIHoverBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource rUIBorderBrush}" />
        </Style>

        <Style Selector="^:pressed /template/ Border#PART_Root">
            <Setter Property="Background" Value="{DynamicResource rUIPressedBrush}" />
        </Style>
    </ControlTheme>

</ResourceDictionary>
